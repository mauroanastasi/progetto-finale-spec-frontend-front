export type Videogame = {
    title: string,
    category: string,
    image: string,
    description: string
    platform: string[],
    releaseYear: number,
    developer: string,
    maxPlayers: number,
    price: number
    rating: number,
};